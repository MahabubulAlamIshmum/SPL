#include<stdio.h>
int main()
{
 /*   int a,b;
    scanf("%d %d",&a,&b);

    int *p1=&a;
    int *p2=&b;
    int sum=*p1+*p2;
    int *p3=&sum;

    printf("sum: %d\n",*p3); *////pointer diye sum kora


  /*  int a=7;
    int *prt=&a;
    int ptr;

    printf("Address of the int pointer: %d\n",ptr);

    ptr++;
    printf("Address of the int pointer: %d\n",ptr);

    ptr=ptr+5;
    printf("Address of the int pointer: %d\n",ptr);

    ptr-=5;
    printf("Address of the int pointer: %d\n",ptr); */ ///pointer increment decrement


 /*   int n=5;
    int arr[5]={10, 25, 97, 123, 177};
    int *p=arr;

    for(int i=0; i<n; i++){
        printf("%d\t", *(p+i));
    }
    printf("\n");
}    */ ///just pointer diye value print

